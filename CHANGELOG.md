# yuque-power-user Skill 变更日志

## 2026-06-10 v2.2.1 上线前修复

修复了 3 个核心必修问题：
- `html-table-advanced.md` 新增 `<colgroup>` 与 `colspan` 对照表（6 种组合 + ❌/✅ 示例）
- `ymd-syntax.md` 折叠块章节新增"不可内嵌表格"红字警告，纠正与 `html-table-advanced.md` 的内部矛盾
- `end-to-end-example.md` 新增 URL 与 namespace / slug 对应关系章节（5 种写法对照 + 解析步骤 + 常见错误）

---

## 2026-06-05 补充：`<td>` 内行内格式与块级格式的分界说明

### 变更文件
- `references/html-table-advanced.md`

### 变更内容

#### 单元格内富文本支持列表

**原文**：
```
- ✅ 加粗/斜体/链接
- ✅ 图片（`![](url)`）
- ✅ `<br>` 换行
```

**修改为**：
```
- ✅ 加粗（`**text**` → ne-bold="true"）/ 斜体（`*text*`）/ 链接（`[text](url)`）
- ✅ 彩色加粗（`<font style="color:#DF2A3F;">**text**</font>`）
- ✅ 图片（`![](url)`）
- ✅ `<br>` 换行
```

#### 块级语法说明补充

**原文**：
```
- ❌ **`- item<br/>- item` 在 `<td>` 内不渲染为列表**（Markdown 列表语法在 HTML 表格单元格内不生效）
```

**修改为**：
```
- ❌ **`- item<br/>- item` 在 `<td>` 内不渲染为列表**（Markdown 块级语法在 `<td>` 内不生效；行内语法如 `**bold**` 正常生效）
```

### 根因

语雀 Markdown→Lake 解析器对 `<td>` 内的内容做了行内/块级区分：行内格式（`**bold**`、`*italic*`、`[link]()`、`<font>`）正常解析为原生格式；块级格式（`- item`、`1. item`、`<ul><li>`、引用、代码块）不解析。

### 影响范围

- 周报 HTML 表格中 `**text**` 加粗的使用（已验证可正常工作）
- 未来在 `<td>` 内使用行内格式的场景

---

## 2026-06-05 修正：Playwright 列宽拖拽能力描述错误

### 问题

之前的文档错误地记录了"Playwright 拖动列分隔线无效"，导致 agent 无法正确执行列宽调整操作。

### 变更文件
- `SKILL.md`（护栏#4）
- `references/playwright-workarounds.md`

### 变更内容

#### SKILL.md 护栏#4

**原文（错误）**：
> 4. **表格列宽不可控**：API、YMD、Playwright 拖动均无法设置列宽。全宽展示（`widthMode`按钮）是唯一可自动化的宽度方案

**修改为（经验证正确）**：
> 4. **列宽拖拽可持久化**：通过 Playwright 拖拽 `.ne-ui-table-resize-right` 手柄可以调整列宽，保存后持久化。全宽展示（`widthMode`按钮）可将表格扩展至页面宽度。两者可结合使用——先拖拽调列宽，再点全宽展示。**不反映在 Markdown body** 中，`skylark_doc_detail` 返回的 markdown 表格仍为 `| --- | --- |`

#### playwright-workarounds.md

**删除错误内容**：
- ❌ "Playwright 拖动列分隔线无效"
- ❌ "全宽模式下无法拖动列宽"
- ❌ "widthMode 仅 columnAdaptation 激活后可见"

**新增正确内容**：
- ✅ 方式一：拖拽调整指定列宽（含完整代码）
- ✅ 方式二：全宽展示（含完整代码）
- ✅ 方式三：拖拽 + 全宽展示组合
- ✅ 正确的 DOM 元素选择器和按钮说明
- ✅ `widthMode` 是图标按钮，默认存在

### 验证过程

1. 在容淼周报文档测试：`hover 效果列表头 → 拖拽列宽 → hover 显示工具栏 → 点击 widthMode 图标 → 保存`
2. 刷新后列宽和全宽效果均保持
3. widthMode 按钮无需先点击其他按钮，默认存在于工具栏

### 影响范围

- 所有需要调整表格列宽的场景
- 超宽表格需要全宽展示的场景

---

## 2026-06-05 表格单元格列表渲染验证

### 变更文件
- `references/html-table-advanced.md`

### 变更内容

#### 1. 修正错误信息：单元格内富文本支持列表

**原文（错误）**：
```
- ✅ 列表（`- ` 无序列表）
- ✅ 代码块
- ✅ 引用
```

**修改为（经验证正确）**：
```
- ✅ Unicode 列表符号（`• item<br/>• item`）
- ✅ 数字列表（`1. item<br/>2. item`）
- ❌ <ul><li> 在 <td> 内不渲染为列表（渲染为 `+ ` 纯文本）
- ❌ - item<br/>- item 在 <td> 内不渲染为列表（Markdown 列表语法在 HTML 表格单元格内不生效）
- ⚠️ 代码块和引用在 <td> 内行为不稳定，建议避免
```

**验证过程**：
1. 先用 `- item<br/>- item` 格式发布到语雀 → 不渲染为列表
2. 改用 `<ul><li>item</li></ul>` 格式 → 渲染为 `+ item` 纯文本，用户截图确认
3. 最终用 `• item<br/>• item`（Unicode 项目符号 U+2022 + br换行） → 正确渲染

**根因**：语雀的 Markdown 解析器在 HTML `<td>` 上下文中不再解析 Markdown 列表语法，也不渲染语义化 HTML 列表元素。只有视觉格式（Unicode 符号 + `<br/>` 换行）能可靠工作。

#### 2. 新增章节：单元格内列表渲染

在"已验证不支持的属性"章节之后，新增独立的"单元格内列表渲染（2026-06 验证）"章节，包含：
- 5种方案的验证结果表格
- 推荐写法的 HTML 代码示例
- `<font>` 内嵌套 `**加粗**` 在 `<td>` 中可正常渲染的说明

### 影响范围
- 周报生成（weekly-report skill）的表格格式
- 任何需要在语雀 HTML 表格单元格内使用列表的场景

### 关联变更
- `weekly-report/references/style-prompt.md` 同步更新了表格单元格列表格式规则

---

## 2026-06-05 修正：单元格内列表格式描述不准确

### 问题
上一条变更（2026-06-05 表格单元格列表渲染验证）将 `• item<br/>• item` 标记为"✅ 推荐方案"，但实际上该方案**只有视觉黑点，没有真正的列表缩进对齐**。用户指出后修正。

### 变更

#### html-table-advanced.md

1. **对比表**：HTML表格"单元格内列表"从 `✅` 改为 `⚠️ 仅视觉模拟，无缩进`
2. **富文本支持列表**：
   - `Unicode 列表符号` 从 `✅` 改为 `⚠️ 有黑点但无缩进对齐，仅视觉模拟`
   - `数字列表` 从 `✅` 改为 `⚠️ 同上，无缩进`
3. **验证结果表**：`• item<br/>• item` 从 `✅ 推荐方案` 改为 `⚠️ 有黑点但无缩进对齐`
4. **新增核心矛盾说明**：需要黄色表头→必须用HTML表格→HTML表格内无法渲染真正的列表
5. **新增替代方案提示**：如需真正列表格式，只能放弃黄色表头改用Markdown表格

#### style-prompt.md

同步将"表格单元格内列表格式"规则从"✅ 正确"改为"⚠️ 当前方案"，明确标注无缩进对齐的限制和核心矛盾。

### 根因
语雀 Markdown→Lake 解析器在 `<td>` 上下文中不再二次解析 Markdown 列表语法，也不渲染 `<ul><li>` 为视觉列表。这是平台限制，非格式语法问题。

---

## 2026-06-09 新增：`<colgroup>` API 列宽 + 修正自适应宽度与全宽展示关系

### 变更

1. **新增 `<colgroup>` 列宽比例**：HTML 表格可通过 `<colgroup><col width="N" />` 设定列宽比例，API 写入 body 即生效，无需 Playwright。建议总和 ≤750px（超 750px 不会被 API 剥离，但标准宽度下容器裁切溢出部分，开全宽展示可正常显示）。format=ymd 读写保留，format=md 读回丢失
2. **修正"互斥"为"独立"**：自适应宽度与全宽展示是两个独立功能，不是互斥也不是前置关系。全宽展示可直接点击工具栏第一个无名 icon 开启，无需先开自适应宽度
3. **明确操作顺序**：colgroup 写比例 → 标宽下拖拽精调 → 开全宽展示（全宽下拖拽可能失效）

### 变更文件

- `SKILL.md`：护栏 #4 拆为 4a(colgroup) + 4b(全宽展示独立于自适应宽度)；决策路由新增列宽综合路径
- `html-table-advanced.md`：新增 colgroup 章节（语法/方向指引/硬约束/组合模板/选型决策树）；更新验证表
- `playwright-workarounds.md`：新增列宽控制方案选择流程图；全宽展示代码加状态检查；修正操作顺序；更新概念关系矩阵
- `end-to-end-example.md`：HTML 表格模板加 colgroup；Step 6 改为 colgroup+Playwright 三步流程

### 修正的错误

| 旧描述 | 正确描述 |
|--------|---------|
| `<colgroup>` 不可用（报错 table only allows tr children） | `<colgroup>` 可用，建议总和 ≤750px |
| 自适应宽度 + 全宽展示互斥 | 自适应宽度与全宽展示独立，非互斥非前置 |
| 操作顺序：全宽 → 拖拽 | 操作顺序：拖拽 → 全宽 |
| colgroup >750px 会被 API 静默剥离 | 超 750px 不剥离，但标准宽度下容器裁切 |

---

## 2026-06-09 v2.2 修正：colgroup 750px 非硬限制 + 自适应宽度非全宽前置条件

### 问题

v2.2 初版中两个关键事实错误：

1. **colgroup 750px 限制描述错误**：文档称"总和不可超过 750px，超出会被 API 静默剥离"。实测 800px colgroup 被 API 完整保留，只是标准宽度下容器（750px + overflow: hidden）裁切溢出部分。开全宽展示后，800px 表格正常显示
2. **自适应宽度与全宽展示关系错误**：文档称"自适应宽度是全宽展示的前置条件，必须先开启自适应宽度才能操作全宽展示"。实测两者完全独立——全宽展示可直接点击工具栏第一个无名 icon（c-tb-width-mode-unfold/fold），无需先开自适应宽度

### 变更文件

- `SKILL.md`：护栏 4a 从"超出会被 API 静默剥离"改为"建议 ≤750px；超 750px 不剥离但容器裁切"；护栏 4b 从"需先开启自适应宽度"改为"与自适应宽度独立，可直接点击"
- `html-table-advanced.md`：硬约束 #1 从"不可超过 750px"改为"建议 ≤750px"；验证表 colgroup>750px 从 ❌ 改为 ⚠️；选型决策树措辞修正
- `playwright-workarounds.md`：核心原理重写，移除"自适应宽度前置"描述；全宽展示代码移除自适应宽度前置步骤；5 个概念详解重写为独立关系；❌ 表 colgroup>750px 改为 ⚠️；拖拽代码注释修正
- `end-to-end-example.md`：Step 6 描述从"750px 内"改为"建议 ≤750px"；流程图护栏 4a 描述修正

### 验证

1. 创建 800px colgroup 表格 → API 保留完整，标准宽度下容器裁切，开全宽展示后正常显示
2. 全宽展示 ON + 自适应宽度 OFF → 表格保持 colgroup 自然宽度（800px），容器不裁切
3. 全宽展示 ON + 自适应宽度 ON → 表格撑满页面宽度（取决于视口宽度）
4. 全宽展示状态保存后刷新仍保持（ne-full-width class 持久化）
5. 全宽展示不修改 colgroup 数据（API 读回值不变）
